
<%= form.inputs do -%>
    <%= form.input :location, :hint => 'School/College/University Name'  %>
    <%= form.input :date_from, :as => :date_picker %>
    <%= form.input :date_to, :as => :date_picker %>
    <nav class="action-bar">
			<%= form.submit 'Save', :class => 'btn' %>
		</nav>
<% end %>