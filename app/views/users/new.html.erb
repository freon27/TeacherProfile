
<section class="span-22 last round-top">
	<h2 class=" banner round-top">Sign Up</h2>
	<div class="content round-bottom">
		<%= semantic_form_for @user do |form| %>
		  <%= render :partial => '/users/form', :locals => { :form => form, :edit_password => true } %>
		  <%= form.submit 'Sign up' %>
		<% end %>
  </div>
</section>