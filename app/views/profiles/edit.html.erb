
	<h2 class=" banner round-top">Edit Profile</h2>
	<div class="content">
		<div class="helper">
			<p>This is where you can set up the basic details of your profile and control whether it's published. When your profile is unpublished, only you will be able to view it.
			</p>
		</div>

		<% if @profile.url_suffix %>
		<p class="url-info">
			The public link for this profile is:<br /><%= link_to('http://teachingprofile.com/profiles/public/' +  CGI.escape(@profile.url_suffix), 'http://teachingprofile.com/profiles/public/' +  CGI.escape(@profile.url_suffix)) %>
		</p>
		<% end %>
		
		
		<%= semantic_form_for @profile, :html => { :multipart => true } do |form| %>
		  <%= render :partial => '/profiles/form', :object => form %>
		  <nav class="action-bar"><%= form.submit 'Save', :class => 'btn positive' %></nav>
		<% end %>
  </div>
